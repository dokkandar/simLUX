# 3D_Factory — state at end of 2026-07-15

**Repo:** `~/workspace/simLUX/3D_Factory` · branch `master` · **NO REMOTE** (see §5)
**Tip:** `ffa8365` · working tree clean · workspace builds · 44 cad_app + full suite pass · launches.

---

## 1. ⭐ THE ONE THING TO DO TOMORROW

**Wire the modifier dispatch on `active_view`.** Everything it needs is built, verified, and
observable. Nothing else is in flight.

```
active_view == TwoD   → the established 2D flow. Untouched. (works today)
active_view == ThreeD → the SAME move command, 3D objects + 3D algorithm
```

**The rule that makes it correct (do not re-derive it — it was learned the hard way):**
- MOVE is MOVE. **One command, six steps.** Never a parallel "3D Move".
- Dispatch on **`active_view`** (last-interacted viewport) — **NEVER** on `factory.open`.
  A visible panel says nothing about what you're editing; gating on it hijacked `m` out of
  the 2D drawing. Twice. A guard test now forbids `factory.open` in `run_command`.
- Memory: `feedback_move_is_move_never_invent_parallel_commands`.

### The exact gap, from the user's 2026-07-15 dump
```
[14010.5] active_view "TwoD" → "ThreeD"     ← 3D active ✓
[15654.8] CMD " m" → Move
[15668.7] select_mode "Off" → "ForSelect"   ← ✗ opened the 2D selection session
[16987.0] NOTE: 3D select → [1]             ← the solid lands in factory.selection
          (the session waits on the 2D basket → the move can never complete)
```
**Step 3 must fill the same basket the apply reads.** Today the 3D pick and the move's
basket are two different places. That is the whole remaining problem.

### What dispatch must do (same command, same prompts)
| Step | 2D (today) | 3D (to build) |
|---|---|---|
| 2. `move`/`m` | established | same command |
| 3. select what is going to move | 2D session over `doc.dobjects` | session gathers from the **3D view** (`factory.selection`) |
| 4. first point (BASE) | canvas pick · osnap ✓ grid ✓ **card ✗** | 3D pick · vertex osnap ✓ **card ✗** |
| 5. second point (DEST) | canvas pick · osnap ✓ grid ✓ **card ✓** + DDE | 3D pick · osnap ✓ **card ✓** |
| 6. done | `apply_move(dest−base)`, clears selection | translate `factory.selection` features, clears selection |

`cad_solid::modify::Modify` already implements the 3D side (spec-conformant, unit-tested).
It is **not wired to anything** — deliberately. Do not resurrect the old intercept; extend
the established flow.

---

## 2. Built + verified today

| | |
|---|---|
| **Draw3D** | 10 solids w/ standard controllers, in a dialog: Box · Sphere · Cylinder · Cone/Frustum · Prism · Pyramid · Capsule · Torus · Tube · Ellipsoid. Cone/Prism/Pyramid are ONE `Primitive::Frustum`. Capsule/Tube are composed (csgrs has neither). |
| **Mouse rule in 3D** | viewpoint change = **middle-drag or Alt+Right ONLY**; left = selector. Test-enforced. |
| **Shared cursors** | `draw_draft_cursor` (square+cross) / `draw_select_cursor` ("^") — ONE impl, used by **both** canvases. |
| **Active view** | `ActiveView{TwoD,ThreeD}`, flips only on real interaction inside a viewport; header pill `◧ 2D/3D active`; **verified correct in the user's dump**. |
| **Recorder** | now watches `active_view` **+ move/copy/rotate/scale/mirror states** — they were never watched, which is why every dump went silent after `CMD "move"`. A MOVE is now fully reconstructable. |
| **Sketch on face** | right-click a face → "Draw on this face" → the app's FULL 2D toolset on that plane (doc-swap). |

## 3. Removed today (do not bring back)
The invented **"3D Move"**: the `run_command` intercept, `FactoryState::modify`, the
ghost/ants/label painters, and the tests that encoded the invention as intent.
`run_command` now has **zero** references to the factory.
> The preview work (selection shade, ghost, marching-ants) was correct per
> `BASIC_MODIFIERS_RULES` §0.6 but hung off the *parallel command*, so it went with it.
> Re-add it **on the established flow** once dispatch lands. The shade survives.

## 4. Owed / known
- **⛔ `HANDLE_COUNTER` collision** — proven: load a file with handles to 1,000,000 → the next
  object drawn gets handle **2**. Corrupts hatch boundaries (`Hatch.boundary_handles` resolves
  BY HANDLE) and exists in RUST_CAD too. Test: `cargo test -p cad_io -- --ignored`.
  Fix ≈5 additive lines: `reserve_handles_above()` called from `read_rsm`.
- **Planes on dobjects** — designed, not built. `PLANES_ON_DOBJECTS_DESIGN_AND_TODO.md`.
  Plane = **ID + 3 global points**; plane 0 = STANDARD = world axes ⇒ 2D is a literal no-op.
  Needs 2 owner decisions (§8): sidecar-vs-kernel (recommend **sidecar**, per decision D5),
  and the v7-reader policy.
- Fillet on a plane (`f` → `r` → `10`) — **never confirmed by the user**. Still the acceptance
  test for the whole fork.
- `mentor MD/` here is a COPY. The originals live in `~/workspace/simLUX/mentor MD/`.

## 5. ⚠️ NOT BACKED UP
This repo has **no remote**; 12 commits exist **only on this disk**. `gh` is authed as
`hsidev` (an HSI account) and the SSH key is a **deploy key scoped to `dokkandar/simLUX`**,
so a new `dokkandar/3D_Factory` cannot be created from here, and pushing elsewhere would
violate the dokkandar-only push policy. To unblock, either:
1. create an empty `dokkandar/3D_Factory` + grant the deploy key push access, or
2. authorise pushing this history to `dokkandar/simLUX` as a `3d-factory` branch.

**simLUX itself is pushed and current** (`origin/simlux-3d-sandbox`).

---

## 6. Run
```bash
cd ~/workspace/simLUX/3D_Factory
cargo run -p cad_app          # 3D Factory ▸ 3D Factory view / Draw3D
cargo test --workspace
cargo test -p cad_io -- --ignored   # the handle-collision bug, on demand
```
